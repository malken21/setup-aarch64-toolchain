# aarch64-gh-actions-runner

A lightweight Docker image for GitHub Actions runners targeting AArch64 Linux.
It includes:
- GitHub CLI (`gh`)
- `jq`
- `binutils` (assembler, linker, etc.)

Used by [malken21](https://github.com/malken21/malken21) to generate language SVGs using AArch64 assembly.
